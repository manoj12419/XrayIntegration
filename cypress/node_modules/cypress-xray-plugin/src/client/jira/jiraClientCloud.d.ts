import { BasicAuthCredentials } from "../../authentication/credentials";
import { JiraClient } from "./jiraClient";
/**
 * A Jira client class for communicating with Jira Cloud instances.
 */
export declare class JiraClientCloud extends JiraClient {
    constructor(apiBaseUrl: string, credentials: BasicAuthCredentials);
    getUrlAddAttachment(issueIdOrKey: string): string;
    getUrlGetIssueTypes(): string;
    getUrlGetFields(): string;
    getUrlPostSearch(): string;
    getUrlEditIssue(issueIdOrKey: string): string;
}
