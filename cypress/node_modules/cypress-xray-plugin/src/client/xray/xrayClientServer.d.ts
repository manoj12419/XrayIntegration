import FormData from "form-data";
import { BasicAuthCredentials, PATCredentials } from "../../authentication/credentials";
import { RequestConfigPost } from "../../https/requests";
import { CucumberMultipartFeature } from "../../types/xray/requests/importExecutionCucumberMultipart";
import { ICucumberMultipartInfo } from "../../types/xray/requests/importExecutionCucumberMultipartInfo";
import { ImportExecutionResponseServer } from "../../types/xray/responses/importExecution";
import { ImportFeatureResponseServer } from "../../types/xray/responses/importFeature";
import { XrayClient } from "./xrayClient";
export declare class XrayClientServer extends XrayClient {
    /**
     * Construct a new Xray Server client using the provided credentials.
     *
     * @param apiBaseUrl - the base URL for all HTTP requests
     * @param credentials - the credentials to use during authentication
     */
    constructor(apiBaseUrl: string, credentials: BasicAuthCredentials | PATCredentials);
    getUrlImportExecution(): string;
    handleResponseImportExecution(response: ImportExecutionResponseServer): string;
    getUrlExportCucumber(issueKeys?: string[], filter?: number): string;
    getUrlImportFeature(projectKey: string): string;
    handleResponseImportFeature(response: ImportFeatureResponseServer): void;
    prepareRequestImportExecutionCucumberMultipart(cucumberJson: CucumberMultipartFeature[], cucumberInfo: ICucumberMultipartInfo): Promise<RequestConfigPost<FormData>>;
    handleResponseImportExecutionCucumberMultipart(response: ImportExecutionResponseServer): string;
}
