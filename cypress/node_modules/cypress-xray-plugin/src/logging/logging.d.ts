export interface LoggingOptions {
    debug?: boolean;
    logDirectory: string;
}
export declare function initLogging(options: LoggingOptions): void;
export declare const logInfo: (...text: string[]) => void;
export declare const logError: (...text: string[]) => void;
export declare const logSuccess: (...text: string[]) => void;
export declare const logWarning: (...text: string[]) => void;
export declare const logDebug: (...text: string[]) => void;
/**
 * Writes arbitrary data to a file under the log path configured in
 * {@link initLogging | `initLogging`}.
 *
 * @param data - the data to write
 * @param filename - the filename to use for the file
 */
export declare function writeFile<T>(data: T, filename: string): string;
/**
 * Writes an error to a file (e.g. HTTP response errors) under the log path configured in
 * {@link initLogging | `initLogging`}.
 *
 * @param error - the error
 * @param filename - the filename to use for the file
 */
export declare function writeErrorFile(error: unknown, filename: string): void;
