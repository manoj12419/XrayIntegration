"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getLatestDate = exports.getEarliestDate = void 0;
/**
 * Retrieve the overall earliest date of multiple dates.
 *
 * @param dates - the dates
 * @returns the earliest date
 */
function getEarliestDate(...dates) {
    let start = null;
    dates.forEach((date) => {
        if (!start || date < start) {
            start = date;
        }
    });
    return start;
}
exports.getEarliestDate = getEarliestDate;
/**
 * Retrieve the overall latest date of multiple dates.
 *
 * @param dates - the dates
 * @returns the latest date
 */
function getLatestDate(...dates) {
    let end = null;
    dates.forEach((date) => {
        if (!end || date > end) {
            end = date;
        }
    });
    return end;
}
exports.getLatestDate = getLatestDate;
